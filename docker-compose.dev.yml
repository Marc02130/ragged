# Overlay for host webpack-dev-server (browser :3000 → proxy → api:8000).
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
services:
  api:
    ports:
      - "8000:8000"
